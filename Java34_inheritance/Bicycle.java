package Java34_inheritance;

public class Bicycle extends Vehicle{
    //the bicycle class will receive the attributes from the vehicle class without listing it twice
    // in order to list , when defining the class, we add the word "extends" and the name of the class
    // now the bicycle class is a subclass of the vehicle class

    int wheels = 2;
    int pedals = 2;
}
