package Java28_variablescope;

public class Main {
    public static void main(String[] args) {

        //local = declared inside a method visible only to that method
        //global = declared outside a method, but within a class visible all parts of the class

        DiceRoller kosci = new DiceRoller();

    }
}
